__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
*.egg-info/
.DS_Store

# Local env / secrets (never commit API keys)
.env

# SQLite database + cached LLM responses (regenerated by seed/ingest)
data/*.db
data/cache/*.json
